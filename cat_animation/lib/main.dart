import 'package:flutter/material.dart';
import 'src/app.dart';
// import 'package:flutter/rendering.dart';
void main(){
  ///[debugPaintSizeEnabled = true] shows some lines on the screen , which shows how layout is rendered.
  // debugPaintSizeEnabled = true; 
  runApp(App());
  }